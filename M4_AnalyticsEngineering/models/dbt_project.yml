name: 'retail_voice_analysis'
version: '1.0.0'
config-version: 2

profile: 'retail_voice'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  retail_voice_analysis:
    staging:
      +materialized: view
    core:
      +materialized: table
